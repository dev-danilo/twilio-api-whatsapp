import Conf from 'conf';
import { ActivateCliFlags } from './activate';
import { DeployCliFlags } from './deploy';
import { ListCliFlags } from './list';
import { StartCliFlags } from './start';
declare type CommandConfigurations = {
    deployConfig: Partial<DeployCliFlags>;
    listConfig: Partial<ListCliFlags>;
    startConfig: Partial<StartCliFlags & {
        serviceSid: string;
    }>;
    activateConfig: Partial<ActivateCliFlags>;
};
export declare function getConfig(baseDir: string, configName?: string): Conf<any>;
export declare type SpecializedConfigOptions = {
    projectId: string;
    environmentSuffix: string;
};
export declare function readSpecializedConfig<T extends keyof CommandConfigurations>(baseDir: string, configFileName: string, commandConfigName: T, opts?: Partial<SpecializedConfigOptions>): CommandConfigurations[T];
export {};
