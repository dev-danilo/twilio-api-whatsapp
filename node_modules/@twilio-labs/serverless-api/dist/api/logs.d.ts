/** @module @twilio-labs/serverless-api/dist/api */
import { GotClient, LogApiResource, Sid } from '../types';
/**
 * Calls the API to retrieve a list of all assets
 *
 * @param {Sid} environmentSid environment in which to get logs
 * @param {Sid} serviceSid service to look for logs
 * @param {GotClient} client API client
 * @returns {Promise<LogApiResource[]>}
 */
export declare function listLogResources(environmentSid: Sid, serviceSid: Sid, client: GotClient): Promise<LogApiResource[]>;
/**
 * Calls the API to retrieve a list of all assets
 *
 * @param {Sid} logSid SID of log to retrieve
 * @param {Sid} environmentSid environment in which to get logs
 * @param {Sid} serviceSid service to look for logs
 * @param {GotClient} client API client
 * @returns {Promise<LogApiResource>}
 */
export declare function getLog(logSid: Sid, environmentSid: Sid, serviceSid: Sid, client: GotClient): Promise<LogApiResource>;
